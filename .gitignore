# Minecraft extracted / decompiled (32k files, 300MB) – extracted/ is now committed as whole jar
# But keep decompiled/ etc ignored for local builds
deobfuscated/
decompiled/
decompiled_*/
*.log
vineflower.jar
# Keep JARs and tarballs for whole jar (required)
!minecraft-client.jar
!minecraft-server*.jar
!minecraft-client-*.tar.gz
!minecraft-client-*.jar
!*.tar.gz

# Gradle / IDE
.gradle/
build/
out/
.idea/
*.iml

# Godot / Android build outputs - CI builds, verifies and publishes the APK.
# Never commit APKs or export logs again (the old 2.4KB placeholder APK must not return).
.godot/
builds/*
*.apk
*.aab
